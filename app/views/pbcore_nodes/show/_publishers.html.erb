<dt>Publishers</dt>
<% document.publisher_name.each_index do |index| %>
  <dd>
    <%= document.publisher_name[index] %>
    <% if document.publisher_role[index] %>
      (<%= document.publisher_role[index] %>)
    <% end %>
  </dd>
<% end %>