<% [:notice, :error, :alert].each do |type| %>
  <% if flash[type] %>
    <div class="alert alert-block">
      <%= flash[type] %>
      <a class="close" data-dismiss="alert" href="#">×</a>
    </div>
  <% end %>
<% end %>
