<h1><%= @video.main_title.first %></h1>

<%= render_submission_workflow_step -%>

<% navbar_items << capture do %>
  <%= edit_and_show_links %>
  <%= button_to 'Delete', archival_videos_path(params[:id]), :class => "btn-small btn-danger", :form => { :class=>"navbar-search"}, :confirm => 'Are you sure?', :method => :delete %>
<% end %>

<% unless params[:new_asset] %>
  <% sidebar_items << capture do %>
    <%= display_sidebar_nav("archival_videos") %>
  <% end %>
<% end %>