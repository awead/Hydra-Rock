<h1>New Contributor</h1>

<%= form_tag(request.fullpath.split(/\//)[0..3].join("/")) do %>
  <%= text_field_tag "name" %>
  <%= select_tag "role", options_for_select(Relators.marc.keys.sort) %>
  <%= submit_tag %>
<% end %>